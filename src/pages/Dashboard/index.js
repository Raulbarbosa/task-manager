import { Header } from "../../components/Header";

export default function Dashboard() {
    return (
        <div>
            <Header name={'Raul'} />
            <h1>Dashboard</h1>
        </div>
    )
};